# netglow-site

Static site for netglow.com, served via GitHub Pages.

- `/` — minimal landing page
- `/privacy` — privacy policy (required by App Store Guideline 5.1.1(i))

Source of truth for the policy text is `docs/PRIVACY_POLICY.md` in the
`netglow-native` app repo — update both when the policy changes.
